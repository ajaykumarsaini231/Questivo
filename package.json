{
  "name": "agentic-mock-test",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "generate": "prisma generate",
    "start": "npm run generate && node server.js",
    "build": "npx prisma generate && (tumhari build command)",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.1.0",
    "@prisma/client": "^7.1.0",
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.19.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prisma": "^7.1.0"
  }
}
